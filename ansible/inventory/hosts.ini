# ============================================================
# Ansible Inventory – Ansible server manages EKS via kubeconfig
# All EKS interactions happen on localhost (the Ansible EC2 node)
# ============================================================

[eks_controller]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
